CREATE TABLE city
{
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name VARCHAR(50),
    CONSTRAINT PK_City PRIMARY KEY(id)
};

CREATE TABLE country
{
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name VARCHAR(50),
    CONSTRAINT PK_Country PRIMARY KEY(id)
};


CREATE TABLE user
{
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name VARCHAR(255),
    email VARCHAR(255),
    password VARCHAR(255),
    nicName VARCHAR(255),
    idCity BIGINT,
    idCountry BIGINT,
    role VARCHAR(10) default 'USER',
    state VARCHAR(10) default 'ACTIVE',
    CONSTRAINT PK_User PRIMARY KEY(id)
};

ALTER TABLE user ADD CONSTRAINT FK_City FOREIGN KEY(idCity) REFERENCES city(id);
ALTER TABLE user ADD CONSTRAINT FK_Country FOREIGN KEY(idCountry) REFERENCES country(id);